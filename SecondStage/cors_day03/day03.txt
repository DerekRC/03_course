template - 模板引擎
模板引擎的原理
1、想要匹配<%= 内容 %>我们定义正则：
    JS中创建正则表达式：
    var reg = /正则/g;
    var reg = new RegExp('正则');
2.正则的含义：
   <%: 以<% 开始
   =\s* :匹配 '='后面的空格
   ([^%>]+\S):匹配所有非空个字符
   \s*%>:匹配多个空格 以%>结尾
   完整的匹配： reg = /<%=\s*([^%>]+\S)\s*%>/g


ArtTemplate - 腾讯
 